[package]
name = "clipmesh-hub-edge"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
clipmesh-hub-core = { path = "../clipmesh-hub-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
toml = "0.8"
uuid = "1"

[dev-dependencies]
tempfile = "3"
