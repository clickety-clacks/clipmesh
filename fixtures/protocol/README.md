# Protocol fixtures

These fixtures use the ClipMesh synthetic fixture namespace: UUIDv4 values
begin with `00000000-0000-4000-8000-` and text is synthetic. They contain no
deployment topology, credential, clipboard capture, or private inventory.

`publish-v1.json` is the canonical version-1 publish example. Its clock is
1700000000000 milliseconds since the Unix epoch.
